module Handler.ClientJSON where
import           Data.Aeson
import           Import



-- postClientR :: Handler Value
-- postClientR = do
  -- a <- requireCheckJsonBody :: Handler Value
  -- returnJson a
  -- let searchResult = case (fromJSON incomingJson) :: Result SearchResult of
                      -- Success a -> a
                      -- Error a   ->  error "failure"

